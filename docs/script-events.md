# Script Events Reference

Script events are the building blocks of game logic in GB Studio. Each event is a JSON object with a `command`, `args`, and optionally `children` (for branching logic).

## Event Structure

```json
{
  "id": "uuid",
  "command": "EVENT_COMMAND_NAME",
  "args": { ... },
  "children": {
    "true": [ ... ],
    "false": [ ... ]
  }
}
```

- **`id`** — Unique UUID (auto-generated by `add_script_event`)
- **`command`** — The event type (see below)
- **`args`** — Command-specific parameters
- **`children`** — Sub-scripts for branching events (only used by conditionals)

Events in a script array execute **sequentially**, top to bottom.

---

## Dialogue & Text

### `EVENT_DIALOGUE`

Display a text dialogue box.

```json
{
  "command": "EVENT_DIALOGUE",
  "args": {
    "text": "Welcome to the village!\nSpeak to the elder for your quest."
  }
}
```

| Arg | Type | Description |
|-----|------|-------------|
| `text` | string | Text to display. Use `\n` for line breaks. Max ~18 chars per line, 4 lines visible. |

### `EVENT_CHOICE`

Show a yes/no choice and branch based on the answer.

```json
{
  "command": "EVENT_CHOICE",
  "args": {
    "trueText": "Yes",
    "falseText": "No"
  },
  "children": {
    "true": [
      { "id": "uuid", "command": "EVENT_DIALOGUE", "args": { "text": "Great choice!" } }
    ],
    "false": [
      { "id": "uuid", "command": "EVENT_DIALOGUE", "args": { "text": "Maybe next time." } }
    ]
  }
}
```

| Arg | Type | Description |
|-----|------|-------------|
| `trueText` | string | Label for the "yes" option |
| `falseText` | string | Label for the "no" option |

### `EVENT_MENU`

Show a multi-option menu and store the selection in a variable.

```json
{
  "command": "EVENT_MENU",
  "args": {
    "variableId": "var-uuid",
    "options": ["Buy Sword", "Buy Shield", "Leave"]
  }
}
```

---

## Scene Navigation

### `EVENT_SCENE_SWITCH`

Transition to a different scene.

```json
{
  "command": "EVENT_SCENE_SWITCH",
  "args": {
    "sceneId": "target-scene-uuid",
    "x": 5,
    "y": 10,
    "direction": "down",
    "fadeSpeed": 2
  }
}
```

| Arg | Type | Description |
|-----|------|-------------|
| `sceneId` | string | UUID of the target scene |
| `x` | number | Player's X tile position in the new scene |
| `y` | number | Player's Y tile position in the new scene |
| `direction` | string | Player's facing direction after transition |
| `fadeSpeed` | number | Fade speed (1-6, lower = faster) |

---

## Variables & Conditionals

### `EVENT_SET_VARIABLE_TRUE`

Set a boolean variable to true.

```json
{ "command": "EVENT_SET_VARIABLE_TRUE", "args": { "variableId": "var-uuid" } }
```

### `EVENT_SET_VARIABLE_FALSE`

Set a boolean variable to false.

```json
{ "command": "EVENT_SET_VARIABLE_FALSE", "args": { "variableId": "var-uuid" } }
```

### `EVENT_VARIABLE_MATH`

Perform math on a variable.

```json
{
  "command": "EVENT_VARIABLE_MATH",
  "args": {
    "variableId": "var-uuid",
    "operation": "add",
    "value": 10
  }
}
```

| Arg | Type | Description |
|-----|------|-------------|
| `variableId` | string | Variable UUID |
| `operation` | string | `"set"`, `"add"`, `"sub"`, `"mul"`, `"div"`, `"mod"` |
| `value` | number | Operand value |

### `EVENT_IF_VARIABLE_TRUE`

Branch based on whether a variable is true.

```json
{
  "command": "EVENT_IF_VARIABLE_TRUE",
  "args": { "variableId": "var-uuid" },
  "children": {
    "true": [ /* events if true */ ],
    "false": [ /* events if false */ ]
  }
}
```

### `EVENT_IF_VARIABLE_VALUE`

Branch based on comparing a variable to a value.

```json
{
  "command": "EVENT_IF_VARIABLE_VALUE",
  "args": {
    "variableId": "var-uuid",
    "operator": ">=",
    "value": 10
  },
  "children": {
    "true": [ /* events if condition met */ ],
    "false": [ /* events if not */ ]
  }
}
```

| Arg | Type | Description |
|-----|------|-------------|
| `operator` | string | `"=="`, `"!="`, `"<"`, `">"`, `"<="`, `">="` |

---

## Actor Control

### `EVENT_ACTOR_MOVE_TO`

Move an actor to a tile position.

```json
{
  "command": "EVENT_ACTOR_MOVE_TO",
  "args": { "actorId": "actor-uuid", "x": 10, "y": 5 }
}
```

### `EVENT_ACTOR_SET_DIRECTION`

Change an actor's facing direction.

```json
{
  "command": "EVENT_ACTOR_SET_DIRECTION",
  "args": { "actorId": "actor-uuid", "direction": "left" }
}
```

### `EVENT_ACTOR_SET_POSITION`

Instantly teleport an actor (no walking animation).

```json
{
  "command": "EVENT_ACTOR_SET_POSITION",
  "args": { "actorId": "actor-uuid", "x": 3, "y": 7 }
}
```

### `EVENT_ACTOR_HIDE` / `EVENT_ACTOR_SHOW`

Toggle actor visibility.

```json
{ "command": "EVENT_ACTOR_HIDE", "args": { "actorId": "actor-uuid" } }
{ "command": "EVENT_ACTOR_SHOW", "args": { "actorId": "actor-uuid" } }
```

---

## Camera

### `EVENT_CAMERA_MOVE_TO`

Move the camera to a tile position.

```json
{
  "command": "EVENT_CAMERA_MOVE_TO",
  "args": { "x": 10, "y": 5, "speed": 2 }
}
```

### `EVENT_CAMERA_LOCK` / `EVENT_CAMERA_SHAKE`

```json
{ "command": "EVENT_CAMERA_LOCK", "args": { "speed": 1 } }
{ "command": "EVENT_CAMERA_SHAKE", "args": { "time": 30 } }
```

---

## Screen Effects

### `EVENT_FADE_IN` / `EVENT_FADE_OUT`

```json
{ "command": "EVENT_FADE_IN", "args": { "fadeSpeed": 2 } }
{ "command": "EVENT_FADE_OUT", "args": { "fadeSpeed": 2 } }
```

| Arg | Type | Description |
|-----|------|-------------|
| `fadeSpeed` | number | 1-6 (1 = fastest) |

### `EVENT_OVERLAY_SHOW` / `EVENT_OVERLAY_HIDE`

```json
{ "command": "EVENT_OVERLAY_SHOW", "args": { "color": "white", "x": 0, "y": 0 } }
{ "command": "EVENT_OVERLAY_HIDE", "args": {} }
```

---

## Timing

### `EVENT_WAIT`

Pause script execution.

```json
{ "command": "EVENT_WAIT", "args": { "time": 30 } }
```

| Arg | Type | Description |
|-----|------|-------------|
| `time` | number | Wait time in frames (60 frames ≈ 1 second) |

---

## Sound & Music

### `EVENT_SOUND_PLAY`

Play a sound effect.

```json
{
  "command": "EVENT_SOUND_PLAY",
  "args": { "type": "beep", "pitch": 4, "duration": 0.5 }
}
```

### `EVENT_MUSIC_PLAY` / `EVENT_MUSIC_STOP`

```json
{ "command": "EVENT_MUSIC_PLAY", "args": { "musicId": "music-uuid" } }
{ "command": "EVENT_MUSIC_STOP", "args": {} }
```

---

## Player

### `EVENT_PLAYER_SET_SPRITE`

Change the player's sprite sheet.

```json
{ "command": "EVENT_PLAYER_SET_SPRITE", "args": { "spriteSheetId": "sprite-uuid" } }
```

---

## Common Patterns

### NPC Dialogue Loop

An NPC that says different things based on game state:

```json
[
  {
    "id": "1", "command": "EVENT_IF_VARIABLE_TRUE",
    "args": { "variableId": "talked-to-elder-uuid" },
    "children": {
      "true": [
        { "id": "2", "command": "EVENT_DIALOGUE", "args": { "text": "Good luck on your quest!" } }
      ],
      "false": [
        { "id": "3", "command": "EVENT_DIALOGUE", "args": { "text": "You should talk to\nthe village elder." } }
      ]
    }
  }
]
```

### Door Trigger (Scene Transition)

A trigger that teleports the player to another scene:

```json
[
  { "id": "1", "command": "EVENT_FADE_OUT", "args": { "fadeSpeed": 2 } },
  {
    "id": "2", "command": "EVENT_SCENE_SWITCH",
    "args": { "sceneId": "dungeon-uuid", "x": 9, "y": 13, "direction": "up", "fadeSpeed": 2 }
  }
]
```

### Item Pickup

An actor that gives the player an item when interacted with:

```json
[
  {
    "id": "1", "command": "EVENT_IF_VARIABLE_TRUE",
    "args": { "variableId": "has-key-uuid" },
    "children": {
      "true": [
        { "id": "2", "command": "EVENT_DIALOGUE", "args": { "text": "You already have\nthe key." } }
      ],
      "false": [
        { "id": "3", "command": "EVENT_DIALOGUE", "args": { "text": "You found the\nDungeon Key!" } },
        { "id": "4", "command": "EVENT_SET_VARIABLE_TRUE", "args": { "variableId": "has-key-uuid" } }
      ]
    }
  }
]
```

### Conditional Guard

A guard NPC that blocks passage unless the player has a specific item:

```json
[
  {
    "id": "1", "command": "EVENT_IF_VARIABLE_TRUE",
    "args": { "variableId": "has-sword-uuid" },
    "children": {
      "true": [
        { "id": "2", "command": "EVENT_DIALOGUE", "args": { "text": "A fine blade!\nYou may pass." } },
        { "id": "3", "command": "EVENT_ACTOR_MOVE_TO", "args": { "actorId": "guard-uuid", "x": 2, "y": 5 } }
      ],
      "false": [
        { "id": "4", "command": "EVENT_DIALOGUE", "args": { "text": "None shall pass\nwithout a weapon!" } }
      ]
    }
  }
]
```

### Shop Menu

A shopkeeper with a purchase menu:

```json
[
  { "id": "1", "command": "EVENT_DIALOGUE", "args": { "text": "Welcome to my shop!" } },
  {
    "id": "2", "command": "EVENT_CHOICE",
    "args": { "trueText": "Buy Sword", "falseText": "Leave" },
    "children": {
      "true": [
        {
          "id": "3", "command": "EVENT_IF_VARIABLE_VALUE",
          "args": { "variableId": "gold-uuid", "operator": ">=", "value": 50 },
          "children": {
            "true": [
              { "id": "4", "command": "EVENT_VARIABLE_MATH", "args": { "variableId": "gold-uuid", "operation": "sub", "value": 50 } },
              { "id": "5", "command": "EVENT_SET_VARIABLE_TRUE", "args": { "variableId": "has-sword-uuid" } },
              { "id": "6", "command": "EVENT_DIALOGUE", "args": { "text": "Here's your sword!\nUse it wisely." } }
            ],
            "false": [
              { "id": "7", "command": "EVENT_DIALOGUE", "args": { "text": "You don't have\nenough gold!" } }
            ]
          }
        }
      ],
      "false": [
        { "id": "8", "command": "EVENT_DIALOGUE", "args": { "text": "Come back anytime!" } }
      ]
    }
  }
]
```
